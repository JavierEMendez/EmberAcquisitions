flask==3.0.3
psycopg2-binary==2.9.10
werkzeug==3.0.3
gunicorn==22.0.0
openpyxl==3.1.5
APScheduler==3.10.4
fpdf2==2.7.9
weasyprint>=62.0
Pillow>=10.0.0
requests>=2.28.0
pandas>=2.0.0
xlrd>=2.0.1
beautifulsoup4>=4.12.0

# --- Acquisitions GIS tab ---------------------------------------------------
# Geometry/projection for the constraint + yield engine, matplotlib for the
# project map and charts baked into the PDF, reportlab for the PDF itself,
# simplekml for Google Earth export. All ship manylinux wheels with their own
# GEOS/PROJ, so the Dockerfile needs no extra apt packages. geopandas and
# folium were in the standalone app's requirements but are imported nowhere -
# not carried over.
shapely>=2.0
pyproj>=3.6
matplotlib>=3.8
reportlab>=4.0
simplekml>=1.3
