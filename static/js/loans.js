/* ============================================================
   Loans & Debt — page JS.
   The page is server-rendered, theme-handling lives in
   templates/_partials/_account_modal.html (shared across every
   dashboard page), so this file is intentionally near-empty.
   ============================================================ */
(function () {
  "use strict";
  // No-op placeholder. Add page-specific behavior here if needed.
})();
